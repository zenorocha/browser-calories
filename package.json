{
  "name": "browser-calories-chrome",
  "version": "1.2.1",
  "description": "Measure your performance budget",
  "main": "src/scripts/popup.js",
  "license": "MIT",
  "dependencies": {
    "byte-size": "^2.0.0",
    "react": "^0.14.8",
    "react-dom": "^0.14.8"
  },
  "devDependencies": {
    "babel-preset-es2015": "^6.6.0",
    "babel-preset-react": "^6.5.0",
    "babelify": "^7.2.0",
    "browserify": "^13.0.0",
    "node-sass": "^3.4.2",
    "onchange": "^2.2.0",
    "parallelshell": "^2.0.0",
    "watchify": "^3.7.0"
  },
  "scripts": {
    "start": "npm run build",
    "build": "npm run build:scripts && npm run build:styles",
    "build:scripts": "browserify src/scripts/popup.js -t [ babelify --presets [ es2015 react ] ] -o dist/scripts/popup.js",
    "build:styles": "node-sass src/styles/popup.scss dist/styles/popup.css -q",
    "watch": "parallelshell 'npm run watch:scripts' 'npm run watch:styles'",
    "watch:scripts": "onchange 'src/scripts/*.js' -- npm run build:scripts",
    "watch:styles": "onchange 'src/styles/*.scss' -- npm run build:styles"
  }
}
